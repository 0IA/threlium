{#- LLM-facing prose sample of LightRAG retrieval for <graph-answer> (enrich → reasoning).
    Kwargs from GraphAnswerView.for_graph_answer_jinja(); not the internal kg_query_context overlay.
-#}
Graph query: {{ formulated_query }}

{% if include_mermaid and (entities or relations) %}
```mermaid
flowchart LR
{% for e in entities %}
    {{ e.id }}["{{ e.name | replace('"', "'") }}"]
{% endfor %}
{% for r in relations %}
    {{ r.src_node }} -->|{{ r.id }}| {{ r.tgt_node }}
{% endfor %}
```
{% endif %}

{% if entities %}
## Entities
{% for e in entities %}
- {{ e.id }}: {{ e.name }} ({{ e.type }}) — {{ e.description }}
{% endfor %}
{% endif %}

{% if relations %}
## Relationships
{% for r in relations %}
- {{ r.id }}: {{ r.src_id }} → {{ r.tgt_id }}{% if r.keywords %} [{{ r.keywords }}]{% endif %} — {{ r.description }}
{% endfor %}
{% endif %}

{% if answer %}
## Answer
{{ answer }}
{% endif %}

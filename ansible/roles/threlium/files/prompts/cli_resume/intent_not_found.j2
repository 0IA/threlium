{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    docs/ARCHITECTURE.md §6.2 / §4.3: cli_resume did not find a prior cli_intent
    in the notmuch thread. -#}
Threlium cli_resume: could not find a CLI intent message in this thread (notmuch). Ensure the thread is indexed and a prior cli_intent step exists.

{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    docs/ARCHITECTURE.md §6.2: cli_resume found intent in thread but JSON payload
    does not parse or lacks required "cli.argv". -#}
Threlium cli_resume: could not parse stored CLI intent JSON in thread.

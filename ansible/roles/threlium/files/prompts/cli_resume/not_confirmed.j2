{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    docs/ARCHITECTURE.md §6.2 / §4.3: cli_resume invoked but HITL reply missing
    or ambiguous. -#}
Threlium cli_resume: user did not confirm the CLI command (no or ambiguous reply).
{% if interpretation %}
Classifier note: {{ interpretation }}
{% endif %}

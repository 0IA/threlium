{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
    docs/ARCHITECTURE.md §6.2: cli_exec emits observation to enrich_fast@localhost.
    mode: sandbox (user scope, read-only OS) or privileged (system scope, uid=0). -#}
Threlium cli_exec: staged observation (mode={{ mode | default('sandbox') }}, privileged={{ privileged | default(false) | lower }}).
{% if cmd_line %}Intent: {{ cmd_line }}

{% endif %}---
{{ prior if prior else "(empty body)" }}
---
Inspect the output for errors, warnings, or anomalies — do not assume success. If something is wrong, diagnose the root cause before retrying; act on any defect you find rather than ignoring it.
If this was a discovery command (rg, find, cat, git grep): summarize existing matches and canonical paths; if you will implement a change, extend those paths rather than adding parallel modules; persist key paths via thread_memory for this thread.

{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
-#}

{#- docs/ARCHITECTURE.md §6.2: deny by local policy (dangerous argv pattern). -#}
Threlium cli_intent: command denied by local policy (dangerous pattern in argv or disallowed construct).

{#- LLM-facing text is English-only: Cyrillic uses roughly 2–3× more tokens per word.
    This Jinja comment is stripped and never sent to the model.
-#}

Response buffer is empty and no content was provided to response_finalize. The user has not yet received a reply. Continue reasoning and provide a response using response_append followed by response_finalize, or call response_finalize with content directly.
